schema_version = 1
[checks.prose]
kind = "static"
description = "README contains accepted sentence and unrelated text remains intact"
scenarios = ["A01"]
argv = ["python3", "scripts/check_readme.py"]
